# Flask Configuration
SECRET_KEY=sua-chave-secreta-super-segura-min-32-caracteres
FLASK_ENV=production

# Database
# Para produção, use PostgreSQL
DATABASE_URL=postgresql://botmanager:senha-forte@localhost:5432/botmanager_db
# Para desenvolvimento, use SQLite (padrão se não definir)
# DATABASE_URL=sqlite:///instance/saas_bot_manager.db

# SyncPay API
SYNCPAY_CLIENT_ID=seu-client-id-syncpay
SYNCPAY_CLIENT_SECRET=seu-client-secret-syncpay

# Platform Split Payment (ID do seu Client para receber comissões)
PLATFORM_SPLIT_USER_ID=seu-client-id-para-split

# Webhook URL (seu domínio em produção)
WEBHOOK_URL=https://seu-dominio.com.br

# Server Port (opcional, padrão 5000)
PORT=5000

